#include<stdio.h>
#include<string.h>
int main()
{
    int t;
    long long int n,ans,i,j,k;
    scanf("%d",&t);
    while(t--)
    {
        scanf("%lld",&n);
        for(i=0;i<n;)
    }
    return 0;
}
