/*This is a programme for finding common letters in the
two given words which is "AMITABH BACHCHAN" and "RAJNIKANTH"
Author- Chetan Anand
*/

#include<stdio.h>
#include<string.h>
int main()
{
    char a[20]="AMITABH BACHCHAN",r[20]="RAJNIKANTH",check[500],ch;
    int i,j,flag,cnt;
    //printf("%the word length of a is %d\n",strlen(a));
    //printf("%the word length of r is %d\n",strlen(r));

    for(i=0;i<strlen(a);i++)
    {

    }

    for(i=0;i<strlen(a);i++)
    {
        if(check)
        {
            check[cnt]=a[i];
            cnt++;
            for(j=0;j<strlen(r);j++)
            {

            }
        }

    }
    return 0;
}
