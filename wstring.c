#include<stdio.h>
#include<string.h>
#define max(a,b) a>b?a:b
#define min(a,b) a<b?a:b

int cnt[110][110];
int rmax[110][110];
int lmax[110][110];
int preprocess(char *s)
{
    int i,j,k;
    for(i=0;s[i]!='\0';i++)
    {
        if()
    }
}


int main()
{
    int t,i,j,k,num;
    char s[11000],ch;
    scanf("%d",&t);
    while(t--)
    {
        scanf("%s",s);
        puts(s);
        num=preprocess(s);

    }

}
