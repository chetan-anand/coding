#include<stdio.h>

#include<stdio.h>
#include<math.h>
#include<string.h>
#include<stdlib.h>
#include<vector>
#include<algorithm>
#include<string.h>
#define max(a,b) a>b?a:b

using namespace std;


int main()
{
    long long int t,i,j,k,n,m;
    long long int a[100009],cnt;
    scanf("%lld",&t);
    while(t--)
    {
        scanf("%lld",&n);
        for(i=0;i<n;i++)
        {
            scanf("%lld",&a);
        }
        printf("%lld\n",(n*(n-1))/2);
    }
    for(i=0;i<)

	return 0;
}
