#include<stdio.h>
#include<string.h>
int main()
{
    int d,t,sl,ss,i;
    scanf("%d",&d);
    char w[1000][1000],x[1001],ch;
    for(i=0;i<d;i++)
    {
        if(i==0){getchar();}
       // printf("a\n");
        gets(w[i]);
       // puts(w[i]);
       // printf("b\n") ;
    }
    scanf("%d",&t);
    for(i=0;i<t;i++)
    {
        if(i==0){getchar();}
        gets(x);

    }


    return 0;
}
