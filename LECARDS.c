#include<stdio.h>
#include<stdlib.h>
#define mod 1000000007
int main()
{
    int t,i,j,temp,n;
    int ans;
    int dp[1002];
    for(i=1;i<=1002;i++)
    {
        n=i;
        if(n%2==0)
        {
            temp=
            for(i=1;i<=n-1;i++)
            {
                ans=(ans<<1)%mod;
            }
        }
    }
    scanf("%d",&t);
    while(t--)
    {
        scanf("%d",&n);
        printf("%d\n"dp[n]);
    }
    return 0;
}
