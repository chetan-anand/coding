#include<stdio.h>
#include<string.h>
#include<math.h>
int ch[1000006];
int main()
{
    int t,i,j,k,a,n,cnt;
    scanf("%d",&t);
    while(t--)
    {
        scanf("%d",&n);
        cnt=0;
        memset(ch,0,sizeof(ch));
        for(i=0;i<n;i++)
        {
            scanf("%d",&m);
            for(j=1;j<=sqrt(m);j++)
            {
                if(ch[j]==1){continue;}
                if()
            }
        }
    }
    return 0;
}
