#include<stdio.h>
#include<string.h>
#include<fstream.h>
class *node
{
    public:
    int ptrnext;
    int key;
    int ptrfor;
}

void createnode
{
    node o1;
}

int main()
{
    int n,i;
    printf("enter the no of data u wanna enter=");
    scanf("%d",&n);
    for(i=0;i<n;i++)
    {
        createnode();
        if(i=0){o1=}
        scanf("%d",&(o1.key));

    }

    return 0;
}
