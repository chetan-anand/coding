#include<stdio.h>
#define n 4
#include<limit.h>

// in this example we take the amount as 17;
// and 4 denomination coin

int main()
{
    // contain the coin denomiantions
    int coin[n]={1,2,5,10};
    int t;int min[100],i,j,k,temp;
    printf("Enter the target amount=");
    scanf("%d",&t);
    for(i=1;i<=t;i++)
    {
        min[i]=INT_MAX;
    }
    for()

    return 0;

}
